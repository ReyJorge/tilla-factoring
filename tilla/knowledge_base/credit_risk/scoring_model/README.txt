Place the authoritative workbook here:

  invoice_financing_scoring_model_anchor_risk_FINAL.xlsx

Copy from (example path on macOS / Google Drive):

  ~/Library/CloudStorage/GoogleDrive-tillman@aktivni-vinne-sklepy.cz/My Drive/PUYCHILL/RISK_MANAGEMENT/invoice_financing_scoring_model_anchor_risk_FINAL.xlsx

Or set env CREDIT_RISK_SCORING_MODEL_PATH to an absolute path on the server.

If the file is missing at runtime, the Python engine uses embedded defaults aligned with the model;
replace the file for audited parameter alignment.
